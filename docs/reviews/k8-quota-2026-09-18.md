# K8 quota runtime review — 18 September 2026

Exact PR41 head: `268193c4844ebcc9b68e8e85a2b4d631bdbdb8a9`. Fork review branch `shared/K8-quota-review`. Tests and this evidence document only; no application or migration changes, hosted identities, emails, provider calls or hosted database connections. These intentionally failing probes are review evidence, not a release candidate.

## Executed Edge handler

```sh
npm exec -- vitest run tests/reviews/parse-schedule-quota.review.test.ts
```

**2 desired-behavior failures, 5 controls passed** (7 tests, 949 ms).

- A synthetic authenticated coach sending `{}` or malformed JSON `{` calls `claim_ai_call('parse-schedule', 40)` once before the request is rejected. Neither reaches the provider. The desired zero-quota-call assertions fail.
- Positive controls pass: a valid coach request claims once then reaches the provider; an unauthenticated caller receives 401 before profile/quota/provider access; a player receives 403 before quota/provider access; a valid request with denied quota returns 429, and quota failure returns 503, both without provider access.
- Cause: `supabase/functions/parse-schedule/index.ts:68–91` claims allowance before parsing/validating the body. Validate input first, then claim immediately before the paid call.

The test transpiles and executes the actual checked-in handler in a VM. Only its two remote imports, Deno registration/configuration, database transport and provider HTTP are replaced. It asserts the table/filter, exact quota arguments, outgoing provider destination, and execution order. Supabase SDK Auth verification and the hosted JWT gateway are outside this test. All provider responses are synthetic and no network function is available inside the handler except the recording substitute.

## Actual-role quota storage check

```sh
node --test tests/reviews/ai-quota.review.test.mjs
```

**1 desired-behavior failure, 3 controls passed** (4 tests). An in-memory PGlite database replays repository `bootstrap.sql` and only `20260918000002_ai_call_quota.sql`. It verifies the disposable marker and authenticated role/UID, rolls each test back, and closes the database.

- Two unrecognized names, `synthetic-not-an-edge-function-a` and `synthetic-not-an-edge-function-b`, both return true and persist separate count=1 rows under the authenticated caller. The assertion requiring rejection with no rows fails.
- Controls pass: 40 known-function claims succeed, claim 41 fails, a second user has an independent allowance and cannot read the first user's rows, direct authenticated table writes are denied, and anonymous claims fail. The bootstrap models legacy explicit function grants, so anonymous denial occurs at the function's `auth.uid()` guard; the test accepts either that guard or ACL rejection.
- Cause: the public definer RPC accepts arbitrary `p_function_name` strings at migration lines51–81. Allowlist supported names and keep quota policy server-controlled. This confirms unnecessary arbitrary-row creation, **not** bypass of the Edge handlers' fixed AI limits.

This is a focused target-migration check, not a complete migration replay, native concurrent-call test, or hosted PostgREST/Auth verification. The cap control is sequential. Full concurrency and platform deployment verification remain separate.

Focused ESLint passed for both new tests. Explicit TypeScript checking including the outside-`src` handler test passed. Both review suites are opt-in, intentionally red, and do not turn known defects into passing release assertions. Logs: `/private/tmp/trak-k8-edge-review.log` and `/private/tmp/trak-k8-quota-db-review.log`.
