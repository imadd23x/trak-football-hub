# PR42 U7/U8 SQL review — 18 September 2026

Exact head: `e34bf5124f2c5ef82a628a5a5e80e2dc7d40872c`, compared with `d0de05760bbba008139f2a72426c62b8a3d20583`. Isolated branch: `shared/U7-U8-review`. The delta adds two SQL suites and their package/runner registration; it changes no application or production migration.

**Conclusion:** the isolation suite passes 11/11, but this does not establish the full posted academy-isolation/departure claims. The checks do exercise some real RLS denials. Three test defects limit that evidence:

1. **U8 substitutes manual departure state for a failed RPC.** [academy_isolation.sql:184–198](../../supabase/tests/academy_isolation.sql#L184) invokes `remove_coach_from_org` with a service-role claim and no admin UID. The fixture also lacks an admin profile. The actual RPC rejects; the catch block edits roster status and coach membership directly. In-memory instrumentation observed `P0001: Not authorized — must be a club admin`. This proves access against the manually constructed state, not that the real departure operation produces it. Add an authenticated academy-admin fixture, require successful departure, then assert the former coach's access. Do not fall back after an error.
2. **Unrelated SQL errors count as authorization denials.** [idenied:43–59](../../supabase/tests/academy_isolation.sql#L43) accepts any error other than its sentinel; [iattempt:73–77](../../supabase/tests/academy_isolation.sql#L73) suppresses all errors. Replacing only the match RPC name with a nonexistent function still passes 11/11: that assertion records `42883` as a successful denial. Require the expected authorization error, and execute the same operation successfully against an authorized control fixture.
3. **The attacking identity has no positive control.** Coach A's own-assessment update at [lines133–147](../../supabase/tests/academy_isolation.sql#L133) does not verify Coach B's identity. B's control at [lines173–179](../../supabase/tests/academy_isolation.sql#L173) expects zero rows. Replacing B's actor selection with NULL still passes 11/11. Give B their own adult roster/history; assert the SQL role/UID and equivalent successful operations before testing foreign access. U8 likewise needs a pre-departure control for the same departing coach.

The roster insertion denial at lines163–166 also includes a foreign `linked_player_id`, so its rejection alone does not isolate the organization-boundary check. This review makes no claim that a production access bypass was found.

The separate adoption suite also has an evidence gap found by source review:
`pg_temp.become()` at `roster_adoption.sql:34` changes JWT claims without changing
the SQL role. Its assessment counts therefore execute as database owner and do
not prove that the linked player can read the history through RLS. The ambiguous
name assertion at line149 also needs to verify both original identities/rows
remain safely reconciled; its current count alone can pass after an arbitrary
namesake is linked or removed. These are limits of the existing opt-in test,
not a claim that its three passing assertions establish the player journey.

## Executed baseline

From the isolated worktree, using the existing untracked dependency symlink:

```sh
npm run test:db:isolation
npm run test:db:roster-adoption
```

- PGlite 0.5.8 / PostgreSQL 18.3: **59 real migrations** and both existing backfill fixtures replayed.
- Isolation command: exit 0, `Passed: academy_isolation.sql`. Inspecting its result table before rollback confirmed **11/11**.
- Adoption command: exit 1, `Roster adoption: 3 of 6 desired assertions failed`. This remains an explicit red review mode; it is not silently counted as a release pass.
- Registration retains isolation in default and parent-upgrade runs, with adoption behind its explicit flag. No runner-registration regression found.
- Logs: `/private/tmp/trak-u7-u8-isolation.log`, `/private/tmp/trak-u7-u8-roster.log`.

## In-memory false-green checks

The following command was executed from the same worktree. It reads source SQL unchanged, replays into a new in-memory database, changes only the test string in memory, reads assertion results before the suite's rollback, and closes the database. No mutation is written into a repository file or a hosted database.

```sh
node --input-type=module <<'NODE'
import {PGlite} from '@electric-sql/pglite';
import {readFile,readdir} from 'node:fs/promises';
const db=new PGlite();
const read=name=>readFile(`supabase/tests/${name}`,'utf8');
try {
  await db.exec(await read('bootstrap.sql'));
  const files=(await readdir('supabase/migrations')).filter(f=>f.endsWith('.sql')).sort();
  for(const f of files){
    if(f==='20260917205027_secure_parent_invites.sql')await db.exec(await read('parent_invite_backfill_setup.sql'));
    if(f==='20260918070209_restrict_pilot_operational_views.sql')await db.exec(await read('pilot_view_backfill_setup.sql'));
    await db.exec(await readFile(`supabase/migrations/${f}`,'utf8'));
    if(f==='20260917205027_secure_parent_invites.sql')await db.exec(await read('parent_invite_backfill_assertions.sql'));
  }
  console.log(`Replayed ${files.length} real migrations in memory`);
  const original=await read('academy_isolation.sql');
  for(const [name,sql]of [
    ['original',original],
    ['missing match RPC test call',original.replace('SELECT public.log_match_for_player(', 'SELECT public.no_such_match_rpc(')],
    ['missing academy B identity',original.replace('SELECT pg_temp.iactor(pg_temp.aid(11));', 'SELECT pg_temp.iactor(NULL);')],
  ]){
    const inspected=sql.replace(/ROLLBACK;\s*$/, 'SELECT description,passed,detail FROM pg_temp.iso_results; ROLLBACK;');
    try{
      const result=await db.exec(inspected);
      const rows=result.flatMap(q=>q.rows??[]).filter(row=>'passed'in row);
      console.log(JSON.stringify({variant:name,assertions:rows.length,passed:rows.filter(r=>r.passed).length,denials:rows.filter(r=>r.detail&&/^U[78]/.test(r.description))}));
    }catch(e){console.log(JSON.stringify({variant:name,error:e.message}));await db.exec('ROLLBACK');}
  }
}finally{await db.close()}
NODE
```

Observed: all three variants report `assertions:11, passed:11`. The original match denial is `P0001`; the nonexistent-RPC variant records `42883`. Removing B's identity leaves the entire suite green.

A separate execution used the same replay and inserted `PERFORM pg_temp.iassert(true, 'OBSERVED departure fallback', SQLSTATE || ': ' || SQLERRM);` at the start of the existing departure catch block, in memory only. Reading that observation before rollback returned `P0001: Not authorized — must be a club admin`. It did not replace or repair the actual RPC.

## Limits

All identities were synthetic fixtures; no hosted API, database, Auth, provider, email or identity probes occurred. No source tests, app code or migrations were edited. The only persistent review change is this document; the dependency symlink stays untracked. These checks are local PGlite evidence, not native concurrency, hosted deployment, or phone-journey verification. They do not validate the suite's introductory claim about previous live reads. The parked P2 test conversion was not touched.
